[project]
name = "mila-wildfires"
version = "0.1.0"
description = "AI for modelling wildfire daily propagation"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "torch>=2.11.0",
    "pyyaml>=6.0",
]
